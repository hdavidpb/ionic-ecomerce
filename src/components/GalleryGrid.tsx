import * as SC from "./styles";

const GalleryGrid = () => {
  return (
    <SC.GalleryContainer>
      <SC.HandleButton>-</SC.HandleButton>
    </SC.GalleryContainer>
  );
};

export default GalleryGrid;
